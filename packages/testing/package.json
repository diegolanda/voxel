{
  "name": "@voxel/testing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"testing build not required\"",
    "lint": "echo \"testing lint pending\"",
    "typecheck": "echo \"testing typecheck pending\"",
    "test": "playwright test",
    "test:install": "playwright install chromium",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
