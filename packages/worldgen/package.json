{
  "name": "@voxel/worldgen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"worldgen build pending\"",
    "lint": "echo \"worldgen lint pending\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"worldgen tests pending\"",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@voxel/domain": "workspace:*",
    "simplex-noise": "^4.0.3"
  }
}
