{
  "name": "@voxel/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"protocol build pending\"",
    "lint": "echo \"protocol lint pending\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"protocol tests pending\"",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.1.5"
  }
}
