{
  "name": "@voxel/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"domain build pending\"",
    "lint": "echo \"domain lint pending\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"domain tests pending\"",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  }
}
