{
  "name": "@voxel/supabase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./environment": "./src/environment.ts"
  },
  "scripts": {
    "build": "echo \"supabase package build pending\"",
    "lint": "echo \"supabase package lint pending\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"supabase package tests pending\"",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "@voxel/domain": "workspace:*",
    "@voxel/protocol": "workspace:*"
  }
}
