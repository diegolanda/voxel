{
  "name": "@voxel/realtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"realtime build pending\"",
    "lint": "echo \"realtime lint pending\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo \"realtime tests pending\"",
    "format": "echo \"format via root tooling\"",
    "format:check": "echo \"format check via root tooling\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "@voxel/domain": "workspace:*",
    "@voxel/protocol": "workspace:*"
  }
}
